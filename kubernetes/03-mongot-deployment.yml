apiVersion: mongodb.com/v1
kind: MongoDBSearch
metadata:
  name: mongodb
  namespace: mongodb
spec:
  resourceRequirements:
    limits:
      cpu: "1"
      memory: 2Gi
    requests:
      cpu: "500m"
      memory: 1Gi
